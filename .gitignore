
# .NET build artifacts
bin/
obj/

# Distribution output (created by build-package scripts)
dist/

# Compiled DLL inside the mod template folder — generated by build scripts,
# never committed so the folder stays a clean installable template.
mod/*/StardewHdtPhysics.dll
